# FLUX.2-klein API

API locale branchée directement sur le runtime officiel `flux2`.

Pré-requis :
- `deploy/flux2-klein-runtime/.env` configuré
- `./setup.sh` exécuté côté runtime

Commandes :

```bash
cp .env.example .env
./up.sh
./status.sh
```
